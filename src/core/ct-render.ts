import type{PatientVolume}from'./volume';import{windowHu}from'./window';export function renderAxial(v:PatientVolume,s:number,l:number,w:number){const[nx,ny,nz]=v.manifest.dimensions,z=Math.max(0,Math.min(nz-1,Math.round(s*(nz-1)))),pixels=new Uint8ClampedArray(nx*ny*4);for(let y=0;y<ny;y++)for(let x=0;x<nx;x++){const hu=v.hu[(z*ny+y)*nx+x]??-1000,g=Math.round(windowHu(hu,l,w)*255),i=(y*nx+x)*4;pixels[i]=pixels[i+1]=pixels[i+2]=g;pixels[i+3]=255}return{width:nx,height:ny,pixels}}
