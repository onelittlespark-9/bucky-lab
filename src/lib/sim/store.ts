import { create } from "zustand";
import type { Breath, ExposureState, FocalSpot, Marker, Mode, PlacementMode, RadiographResult, Recumbency, RoomEquipment, Screen, SimPose, TubeState } from "./types";
import { patientById, PATIENTS } from "./patients";
import { projectionById, PROJECTIONS, scaleLandmarkY } from "./projections";
import { predictEI, suggestedTechnique } from "./exposure";
import { renderRadiograph, preloadRadiographAssets } from "./render-radiograph";
import type { PathologyId } from "./requests";
import { REQUEST_BANK, requestFromBank } from "./request-bank";
import { clampStandingPatientY, recommendedBuckyHeight, UPRIGHT_IR_HEIGHT } from "./room-geometry";
preloadRadiographAssets(PROJECTIONS);
function defaultEquipment(placement: PlacementMode = "table", projectionId = "pa-chest", patientId = "amara"): RoomEquipment { const patient=patientById(patientId),projection=projectionById(projectionId),buckyHeight=recommendedBuckyHeight(patient.heightCm,scaleLandmarkY(projection.cr.y,patient.heightCm)); if(placement==="upright-bucky"||placement==="standing")return {patientX:0,patientY:0,patientZ:0,tableHeight:.9,tableX:0,tableZ:0,buckyHeight,buckyTilt:0,placement}; if(placement==="seated")return {patientX:0,patientY:-.35,patientZ:0,tableHeight:.9,tableX:0,tableZ:0,buckyHeight:Math.max(UPRIGHT_IR_HEIGHT,buckyHeight*.75),buckyTilt:0,placement}; return {patientX:0,patientY:0,patientZ:0,tableHeight:.9,tableX:0,tableZ:0,buckyHeight,buckyTilt:0,placement:"table"}; }
function placementFromProjection(id:string):PlacementMode{return projectionById(id).setup==="wall"?"upright-bucky":"table";}
function presentedPose(projectionId:string,placement?:PlacementMode):SimPose{const place=placement??placementFromProjection(projectionId),erect=place==="standing"||place==="upright-bucky"||place==="seated";return {recumbency:erect?"erect":"supine",rotationY:0,oblique:0,chinUp:.15,shoulderRoll:.1,armRaise:0,elbowFlex:8,kneeFlex:place==="seated"?90:0,hipInternal:0,breath:"expiration"};}
function presentedTube(projectionId:string,patientId:string,mode:Mode):TubeState{const p=projectionById(projectionId),patient=patientById(patientId),jitter=(n:number)=>(Math.random()-.5)*n;return {crY:scaleLandmarkY(p.cr.y,patient.heightCm)+(mode==="assessment"?jitter(8):0),crX:p.cr.x*(p.anatomy==="torso-ap"||p.anatomy==="torso-lat"?patient.morph.torsoWidth:1)+(mode==="assessment"?jitter(4):0),sid:p.sidCm,angle:p.tubeAngle,collimationW:p.collimationW,collimationH:p.collimationH,lockedToDetector:true};}
function defaultExposure(projectionId:string,patientId:string):ExposureState{const p=projectionById(projectionId),patient=patientById(patientId);let kvp=p.kvp,mas=p.mas;try{const sug=suggestedTechnique(patient,p);kvp=sug.kvp;mas=sug.mas;}catch{}return {kvp,mas,grid:p.grid,focalSpot:p.setup==="tabletop"?"fine":"broad",marker:"R"};}
function assessmentExposure(projectionId:string):ExposureState{const p=projectionById(projectionId);return {kvp:p.kvp,mas:p.mas,grid:p.grid,focalSpot:p.setup==="tabletop"?"fine":"broad",marker:"R"};}
function playConsoleSound(kind:"prep"|"expose"){try{const ctx=new AudioContext(),osc=ctx.createOscillator(),gain=ctx.createGain();osc.connect(gain);gain.connect(ctx.destination);if(kind==="prep"){osc.frequency.value=90;osc.type="sawtooth";gain.gain.setValueAtTime(.02,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.06,ctx.currentTime+.4);osc.start();osc.stop(ctx.currentTime+1.2);}else{osc.frequency.value=880;osc.type="square";gain.gain.setValueAtTime(.05,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.18);osc.start();osc.stop(ctx.currentTime+.2);}window.setTimeout(()=>void ctx.close(),1500);}catch{}}
function handbookPose(projectionId:string):SimPose{const p=projectionById(projectionId),lateral=p.anatomy.includes("lat")||p.name.toLowerCase().includes("lateral");return {recumbency:p.recumbency,rotationY:lateral?(p.laterality==="right"?-90:90):0,oblique:0,chinUp:(p.id==="pa-chest"||p.id.includes("cspine"))?.75:.35,shoulderRoll:p.id==="pa-chest"?.85:.2,armRaise:p.id==="lat-chest"?1:0,elbowFlex:p.id==="ap-elbow"?0:15,kneeFlex:p.id==="lat-knee"?25:0,hipInternal:p.id==="ap-pelvis"||p.id==="ap-hip"?18:0,breath:p.respiration??"inspiration"};}
function handbookTube(projectionId:string,patientId:string):TubeState{const p=projectionById(projectionId),patient=patientById(patientId);return {crY:scaleLandmarkY(p.cr.y,patient.heightCm),crX:p.cr.x*(p.anatomy==="torso-ap"||p.anatomy==="torso-lat"?patient.morph.torsoWidth:1),sid:p.sidCm,angle:p.tubeAngle,collimationW:p.collimationW,collimationH:p.collimationH,lockedToDetector:true};}
export interface SimStore{screen:Screen;mode:Mode;patientId:string;projectionId:string;requestId:string|null;pathologyId:PathologyId;viewIndex:number;completedRequestIds:string[];pose:SimPose;tube:TubeState;exposure:ExposureState;equipment:RoomEquipment;showLandmarks:boolean;showLightField:boolean;setShowLandmarks:(v:boolean)=>void;setShowLightField:(v:boolean)=>void;preparing:boolean;exposing:boolean;result:RadiographResult|null;error:string|null;setScreen:(s:Screen)=>void;setMode:(m:Mode)=>void;setPatient:(id:string)=>void;setProjection:(id:string)=>void;startExam:(projectionId:string,patientId?:string,requestId?:string,viewIndex?:number)=>void;confirmSetup:(placement:PlacementMode)=>void;patchPose:(p:Partial<SimPose>)=>void;patchTube:(t:Partial<TubeState>)=>void;patchExposure:(e:Partial<ExposureState>)=>void;patchEquipment:(e:Partial<RoomEquipment>)=>void;fitBuckyToPatient:()=>void;applyHandbook:()=>void;applySuggestedFactors:()=>void;setLandmarkCR:(landmarkY:number,landmarkX:number)=>void;predictedEI:()=>number;prepare:()=>void;expose:()=>Promise<void>;retake:()=>void;nextRequestedView:()=>void;completeCurrentRequest:()=>void;completeRequestById:(id:string)=>void;startNextRequest:()=>void;resetRequests:()=>void;}
function safeProjectionId(id:string){return PROJECTIONS.some(p=>p.id===id)?id:"pa-chest";}
export const useSim=create<SimStore>((set,get)=>({screen:"library",mode:"practice",patientId:"amara",projectionId:"pa-chest",requestId:null,pathologyId:"none",viewIndex:0,completedRequestIds:[],pose:presentedPose("pa-chest"),tube:presentedTube("pa-chest","amara","practice"),exposure:defaultExposure("pa-chest","amara"),equipment:defaultEquipment("upright-bucky","pa-chest","amara"),showLandmarks:true,showLightField:true,preparing:false,exposing:false,result:null,error:null,
setScreen:screen=>set({screen}),setMode:mode=>{const {projectionId,patientId}=get();set({mode,exposure:mode==="assessment"?assessmentExposure(projectionId):defaultExposure(projectionId,patientId),showLandmarks:mode==="practice"});},setShowLandmarks:showLandmarks=>set({showLandmarks}),setShowLightField:showLightField=>set({showLightField}),setPatient:patientId=>{const {projectionId,equipment}=get();set({patientId,equipment:defaultEquipment(equipment.placement,projectionId,patientId)});},setProjection:projectionId=>{const {patientId,mode}=get(),pid=safeProjectionId(projectionId);set({projectionId:pid,pose:presentedPose(pid),tube:presentedTube(pid,patientId,mode),exposure:mode==="assessment"?assessmentExposure(pid):defaultExposure(pid,patientId),equipment:defaultEquipment(placementFromProjection(pid),pid,patientId),result:null,viewIndex:0});},startExam:(projectionId,patientId,requestId,viewIndex=0)=>{const pid=patientId??get().patientId,{mode}=get(),safeId=safeProjectionId(projectionId),req=requestId?requestFromBank(requestId):null,place=placementFromProjection(safeId);set({projectionId:safeId,patientId:pid,requestId:requestId??null,pathologyId:req?.pathologyId??"none",viewIndex,pose:presentedPose(safeId,place),tube:{...presentedTube(safeId,pid,mode),...handbookTube(safeId,pid),lockedToDetector:true},exposure:mode==="assessment"?assessmentExposure(safeId):defaultExposure(safeId,pid),equipment:defaultEquipment(place,safeId,pid),result:null,error:null,screen:"setup",preparing:false,showLandmarks:mode==="practice"});},confirmSetup:placement=>{const {projectionId,patientId}=get();set({equipment:defaultEquipment(placement,projectionId,patientId),pose:presentedPose(projectionId,placement),screen:"room"});},patchPose:p=>set({pose:{...get().pose,...p}}),patchTube:t=>set({tube:{...get().tube,...t}}),patchExposure:e=>set({exposure:{...get().exposure,...e}}),patchEquipment:e=>{const current=get().equipment,next={...current,...e},upright=next.placement==="upright-bucky"||next.placement==="standing"||next.placement==="seated";if(upright){next.patientY=next.placement==="seated"?Math.max(-.35,Math.min(.1,next.patientY)):clampStandingPatientY(next.patientY);next.patientZ=Math.max(-.12,Math.min(.35,next.patientZ));next.buckyHeight=Math.max(.32,Math.min(1.85,next.buckyHeight));}else next.patientY=Math.max(-.02,Math.min(.35,next.patientY));set({equipment:next});},fitBuckyToPatient:()=>{const {patientId,projectionId,equipment}=get();const p=projectionById(projectionId),patient=patientById(patientId);if(equipment.placement!=="upright-bucky"&&equipment.placement!=="standing"&&equipment.placement!=="seated")return;set({equipment:{...equipment,buckyHeight:recommendedBuckyHeight(patient.heightCm,scaleLandmarkY(p.cr.y,patient.heightCm))}});},applyHandbook:()=>{const {projectionId,patientId}=get();set({pose:handbookPose(projectionId),tube:handbookTube(projectionId,patientId)});get().fitBuckyToPatient();},applySuggestedFactors:()=>{const {projectionId,patientId,exposure}=get();try{const sug=suggestedTechnique(patientById(patientId),projectionById(projectionId));set({exposure:{...exposure,...sug,grid:projectionById(projectionId).grid}});}catch{}},setLandmarkCR:(landmarkY,landmarkX)=>set({tube:{...get().tube,crY:landmarkY,crX:landmarkX}}),predictedEI:()=>{const s=get();try{return predictEI(patientById(s.patientId),projectionById(s.projectionId),s.exposure,s.tube);}catch{return 250;}},prepare:()=>{playConsoleSound("prep");set({preparing:true});window.setTimeout(()=>{if(get().preparing)set({preparing:false});},4000);},expose:async()=>{const s=get();if(s.exposing)return;playConsoleSound("expose");set({exposing:true,error:null,preparing:false});try{const result=await renderRadiograph({patient:patientById(s.patientId),projection:projectionById(s.projectionId),pose:s.pose,tube:s.tube,exposure:s.exposure,pathologyId:s.pathologyId});set({result,screen:"viewer",exposing:false});}catch(err){set({exposing:false,error:err instanceof Error?err.message:"Exposure failed"});}},retake:()=>set({screen:"room",result:null}),nextRequestedView:()=>{const s=get(),req=s.requestId?requestFromBank(s.requestId):undefined,nextIndex=s.viewIndex+1,nextProjection=req?.requestedProjections[nextIndex];if(!req||!nextProjection){set({screen:"room",result:null});return;}get().startExam(nextProjection,req.patientId,req.id,nextIndex);},completeCurrentRequest:()=>{const s=get();if(!s.requestId){set({screen:"library",result:null});return;}const req=requestFromBank(s.requestId);if(req&&s.viewIndex<req.requestedProjections.length-1)return;const completed=s.completedRequestIds.includes(s.requestId)?s.completedRequestIds:[...s.completedRequestIds,s.requestId];set({completedRequestIds:completed,result:null,screen:"library"});},completeRequestById:id=>{if(!REQUEST_BANK.some(r=>r.id===id))return;const done=get().completedRequestIds.includes(id)?get().completedRequestIds:[...get().completedRequestIds,id];set({completedRequestIds:done});},startNextRequest:()=>{const s=get(),next=REQUEST_BANK.find(r=>!s.completedRequestIds.includes(r.id));if(!next){set({screen:"library",result:null});return;}get().startExam(next.requestedProjections[0]??"pa-chest",next.patientId,next.id,0);},resetRequests:()=>set({completedRequestIds:[],requestId:null,result:null,screen:"library",viewIndex:0})}));
export { PATIENTS, PROJECTIONS }; export type { Recumbency, Breath, FocalSpot, Marker };
