import type {CtVolume} from "../ct/ct-volume";
import type {MaterialPath} from "./nist-attenuation";
import {classifyCtHu,materialId,integrateMaterialVolume,type MaterialVolume,type VolumeRay} from "./voxel-volume-projector";
const cache=new WeakMap<CtVolume,MaterialVolume>();
export function ctToMaterialVolume(ct:CtVolume):MaterialVolume{const prior=cache.get(ct);if(prior)return prior;const[nx,ny,nz]=ct.manifest.dimensions,[sx,sy,sz]=ct.manifest.spacingMm,material=new Uint8Array(ct.voxels.length),density=new Uint8Array(ct.voxels.length);for(let i=0;i<ct.voxels.length;i++){const c=classifyCtHu(ct.voxels[i]??-1000);material[i]=materialId(c.material);density[i]=Math.max(0,Math.min(255,Math.round(c.density/1.35*255)));}const origin=ct.manifest.originMm??[-nx*sx/2,-ny*sy/2,-nz*sz/2],out:MaterialVolume={nx,ny,nz,spacingCm:[sx/10,sy/10,sz/10],originCm:[origin[0]/10,origin[1]/10,origin[2]/10],material,density};cache.set(ct,out);return out;}
export function integrateCtVolume(ct:CtVolume,ray:VolumeRay):MaterialPath{return integrateMaterialVolume(ctToMaterialVolume(ct),ray);}
export function projectCtDrr(ct:CtVolume,width:number,height:number,axis:"ap"|"lateral"="ap"){const v=ctToMaterialVolume(ct),out=new Float32Array(width*height),[sx,sy,sz]=v.spacingCm,sizeX=v.nx*sx,sizeY=v.ny*sy,sizeZ=v.nz*sz;for(let py=0;py<height;py++)for(let px=0;px<width;px++){const u=(px+.5)/width-.5,w=(py+.5)/height-.5,ray:VolumeRay=axis==="ap"?{originCm:[v.originCm[0]+sizeX*(u+.5),v.originCm[1]+sizeY*(w+.5),v.originCm[2]-2],direction:[0,0,1]}:{originCm:[v.originCm[0]-2,v.originCm[1]+sizeY*(w+.5),v.originCm[2]+sizeZ*(u+.5)],direction:[1,0,0]},p=integrateMaterialVolume(v,ray);out[py*width+px]=(p.soft??0)+(p.muscle??0)+(p.blood??0)+(p.brain??0)+(p.adipose??0)*.8+(p.inflatedLung??0)*.18+(p.trabecularBone??0)*1.8+(p.corticalBone??0)*3.2;}return out;}
