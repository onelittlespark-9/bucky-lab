import{useEffect,useRef}from'react';import type{PatientVolume}from'../core/volume';import{renderAxial}from'../core/ct-render';export function VolumeCanvas({volume,slice,level,width}:{volume:PatientVolume;slice:number;level:number;width:number}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const r=renderAxial(volume,slice,level,width);c.width=r.width;c.height=r.height;const ctx=c.getContext('2d');if(ctx)ctx.putImageData(new ImageData(r.pixels,r.width,r.height),0,0)},[volume,slice,level,width]);return <canvas ref={ref} className="ct-canvas"/>}
