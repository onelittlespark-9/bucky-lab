import { Html, OrbitControls } from "@react-three/drei";
import { useThree } from "@react-three/fiber";
import { useRef, type CSSProperties, type MutableRefObject } from "react";
import * as THREE from "three";
import { useSim } from "@/lib/sim/store";
import { patientById } from "@/lib/sim/patients";
import { projectionById } from "@/lib/sim/projections";

const clamp=(v:number,min:number,max:number)=>Math.max(min,Math.min(max,v));
type FocusPoint=[number,number,number];
function focusForProjection(region:string,anatomy:string):{target:FocusPoint;distance:number}{if(region==="Head & neck"||anatomy.includes("skull"))return{target:[0,1.62,0],distance:1.45};if(region==="Upper limb")return{target:[0,1.0,0],distance:1.35};if(region==="Lower limb")return{target:[0,.45,0],distance:1.35};if(region==="Pelvis & hips")return{target:[0,.72,0],distance:1.5};if(region==="Spine")return{target:[0,1.05,0],distance:1.55};if(region==="Abdomen")return{target:[0,.92,0],distance:1.55};return{target:[0,1.2,0],distance:1.55}}
function CameraFocus({focusRef}:{focusRef:MutableRefObject<((target:FocusPoint,distance:number)=>void)|null>}){const{camera,controls}=useThree();focusRef.current=(target,distance)=>{const c=controls as any,t=new THREE.Vector3(...target),direction=new THREE.Vector3().subVectors(camera.position,c?.target??t);direction.y*=.25;if(direction.lengthSq()<.01)direction.set(0,.12,1);direction.normalize();camera.position.copy(t).addScaledVector(direction,distance);if(c?.target)c.target.copy(t);c?.update?.()};return null}
export function RoomQuickControls(){const tube=useSim(s=>s.tube),projectionId=useSim(s=>s.projectionId),patientId=useSim(s=>s.patientId),showLightField=useSim(s=>s.showLightField),setShowLightField=useSim(s=>s.setShowLightField),patchTube=useSim(s=>s.patchTube),projection=projectionById(projectionId),patient=patientById(patientId),focusRef=useRef<((target:FocusPoint,distance:number)=>void)|null>(null);const focus=()=>{const f=focusForProjection(projection.region,projection.anatomy);focusRef.current?.(f.target,f.distance)};const focusBeam=()=>{focusRef.current?.([tube.crX/100,clamp(tube.crY/100,.25,1.8),0],1.25)};const nudge=(dx:number,dy:number)=>patchTube({crX:clamp(tube.crX+dx,-20,20),crY:clamp(tube.crY+dy,0,patient.heightCm)});const setField=(factor:number)=>patchTube({collimationW:clamp(projection.collimationW*factor,5,45),collimationH:clamp(projection.collimationH*factor,5,45)});return <><OrbitControls makeDefault enablePan minPolarAngle={.2} maxPolarAngle={Math.PI/2.05} minDistance={.75} maxDistance={6} target={[0,.9,0]}/><CameraFocus focusRef={focusRef}/><Html position={[-1.55,2.05,0]} transform={false} style={{pointerEvents:"auto"}}><div style={{width:230,background:"rgba(8,12,15,.92)",border:"1px solid rgba(255,255,255,.12)",borderRadius:12,padding:10,color:"white",fontFamily:"system-ui",fontSize:11,boxShadow:"0 10px 30px rgba(0,0,0,.35)"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:7}}><strong style={{fontSize:12}}>QUICK POSITIONING</strong><span style={{opacity:.55}}>no sliders</span></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:6}}><button onClick={focus} style={buttonStyle}>Focus exam</button><button onClick={focusBeam} style={buttonStyle}>Focus CR</button><button onClick={()=>patchTube({crX:projection.cr.x*(projection.anatomy.startsWith("torso")?patient.morph.torsoWidth:1),crY:projection.cr.y})} style={buttonStyle}>Centre CR</button><button onClick={()=>patchTube({sid:projection.sid})} style={buttonStyle}>Standard SID</button></div><div style={sectionStyle}>CR NUDGE</div><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:5}}><span/><button onClick={()=>nudge(0,1)} style={buttonStyle}>▲</button><span/><button onClick={()=>nudge(-1,0)} style={buttonStyle}>◀</button><button onClick={()=>nudge(0,-1)} style={buttonStyle}>●</button><button onClick={()=>nudge(1,0)} style={buttonStyle}>▶</button></div><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:5,marginTop:5}}><button onClick={()=>nudge(-5,0)} style={buttonStyle}>−5 X</button><button onClick={()=>nudge(0,-5)} style={buttonStyle}>−5 Y</button><button onClick={()=>nudge(5,0)} style={buttonStyle}>+5 X</button></div><div style={sectionStyle}>COLLIMATION</div><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:5}}><button onClick={()=>setField(.72)} style={buttonStyle}>Tight</button><button onClick={()=>setField(1)} style={buttonStyle}>Standard</button><button onClick={()=>setField(1.2)} style={buttonStyle}>Wide</button></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:5,marginTop:5}}><button onClick={()=>patchTube({collimationW:clamp(tube.collimationW-.5,5,45),collimationH:clamp(tube.collimationH-.5,5,45)})} style={buttonStyle}>Field −</button><button onClick={()=>patchTube({collimationW:clamp(tube.collimationW+.5,5,45),collimationH:clamp(tube.collimationH+.5,5,45)})} style={buttonStyle}>Field +</button></div><div style={sectionStyle}>LIGHT FIELD</div><button onClick={()=>setShowLightField(!showLightField)} style={{...buttonStyle,width:"100%",background:showLightField?"rgba(255,235,130,.2)":"rgba(255,255,255,.06)",borderColor:showLightField?"rgba(255,235,130,.6)":"rgba(255,255,255,.12)"}}>{showLightField?"● PRIMARY BEAM LIGHT FIELD ON":"○ LIGHT FIELD OFF"}</button><p style={{margin:"7px 0 0",lineHeight:1.35,opacity:.58}}>Turn the light field on, then drag the four yellow corner handles directly on the anatomy to collimate. Focus the exam first for easier positioning.</p></div></Html></>}
const buttonStyle:CSSProperties={background:"rgba(255,255,255,.07)",color:"white",border:"1px solid rgba(255,255,255,.12)",borderRadius:7,padding:"6px 5px",fontSize:10,cursor:"pointer"};const sectionStyle:CSSProperties={fontSize:9,letterSpacing:".12em",opacity:.55,margin:"10px 0 5px"};
